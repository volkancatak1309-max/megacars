# dependencies
node_modules/

# build output
dist/
.vercel/

# local scratch & tooling (NOT deployed)
_work/
.claude/
.impeccable/

# misc
*.log
.DS_Store
Thumbs.db

# NOTE: public/cars, public/hero and cars-data.json are intentionally tracked
# (the site needs the real photos, hero video and inventory data at runtime).
